%span{:style => "text-decoration: #{considered(result_counter, result) ? 'none' : 'line-through'}; #{'font-weight: normal;' unless considered(result_counter, result)}"}
  %a{href: params[:age_class] == 'All' ? race_results_url(result.race) : fis_race_href(result.race.href), 'data-toggle' => :tooltip, title: true, 'data-original-title' => details(result)}
    ="#{result.rank || result.failure}#{'*' if result.race.factor > 1}"
