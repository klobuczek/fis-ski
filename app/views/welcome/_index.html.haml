%h2 Standings for the #{params[:season] || Season.current} season
%div{:align => 'left'}
  Previous seasons:
  =render :partial => 'welcome/season', :collection => (Season.fmc_earliest..Season.current.to_i).to_a
-['Men', 'Ladies'].each do |gender|
  %div
    ="#{gender}:"
    =render :partial => 'welcome/age_class', :collection => (1..13).to_a, :locals => { :gender => gender[0,1]}